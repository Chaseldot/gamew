{
  "name": "gamew-universe-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "serve": "python3 -m http.server 5174 --directory dist"
  }
}
